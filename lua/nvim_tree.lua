require("nvim-tree").setup(
    {
        view  = {
            float = {
                enable = false,
            }
        },
        diagnostics = {
            enable = true,
        }
    }
);
